import { Request, Response } from "express";
import conexion from "../config/database";

const menuAlumnosGet=async(req:Request,res:Response)=>{

}
const menuAdminGet=async(req:Request,res:Response)=>{
    
}
const menuProfesorGet=async(req:Request,res:Response)=>{
    
}
const menuSecretariaGet=async(req:Request,res:Response)=>{
    
}

export {menuAlumnosGet,menuAdminGet,menuProfesorGet,menuSecretariaGet}